source 'https://rubygems.org'

# 文件處理相關（移除與主 Redmine 重複的 gem）
# gem 'rubyzip', '~> 2.3'  # 已由主 Redmine 提供
# gem 'nokogiri', '~> 1.15'  # 已由主 Redmine 提供
gem 'roo', '~> 2.10'
gem 'pdf-reader', '~> 2.11'

# 非同步處理
gem 'sidekiq', '~> 7.0'

# 快取
gem 'redis', '~> 5.0'

# 通知
gem 'slack-notifier', '~> 2.4'

# 注意：開發和測試 gem 已移除，因為與主 Redmine 衝突
# 這些 gem 只在開發環境中使用，生產環境不需要
# 注意：axlsx 已移除以避免與 rubyzip 版本衝突 